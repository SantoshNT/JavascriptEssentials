let numbers = [1,2,3,4];
let k = numbers.unshift(); //item added at the begining

let k = numbers.push();//item added at the last

let k = numbers.pop();//item removed at the end

let k = numbers.shift();//item remove at the begining

for (let k of numbers)
console.log(k);